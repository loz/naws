h1. NAWS - Not A WebServer

I wrote a ruby web server which is rack enabled, to see what it would take.

h2. Using in a rails app

* Add 'naws' to your Gemfile
* Add require 'naws' to your script/rails
* run @rails s naws@

